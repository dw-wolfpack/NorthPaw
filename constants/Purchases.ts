/**
 * Configure these in App Store Connect + RevenueCat dashboard.
 * Offerings should include a package that unlocks entitlement `ENTITLEMENT_PRO`.
 */
export const ENTITLEMENT_PRO = 'pro';

/** Monthly subscription product identifier (example, replace in dashboard). */
export const PRODUCT_PRO_MONTHLY = 'northpaw_pro_monthly';

/** Annual subscription product identifier (example, replace in dashboard). */
export const PRODUCT_PRO_ANNUAL = 'northpaw_pro_annual';
